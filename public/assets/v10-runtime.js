(()=>{document.querySelectorAll("[data-v10-carousel]").forEach(carousel=>{const slides=[...carousel.querySelectorAll("[data-v10-slide]")];if(!slides.length)return;let index=Math.max(0,slides.findIndex(slide=>slide.classList.contains("is-active")));const show=next=>{index=(next+slides.length)%slides.length;slides.forEach((slide,i)=>{const active=i===index;slide.classList.toggle("is-active",active);slide.hidden=!active;if(active){slide.querySelectorAll("iframe[data-src]").forEach(frame=>{if(!frame.getAttribute("src")){frame.setAttribute("src",frame.dataset.src||"");frame.removeAttribute("data-src")}})}})};carousel.querySelector("[data-prev]")?.addEventListener("click",()=>show(index-1));carousel.querySelector("[data-next]")?.addEventListener("click",()=>show(index+1));show(index)})})();
